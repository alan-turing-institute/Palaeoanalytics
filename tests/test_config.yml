lithic:
  threshold: 0.8
  contour_parameter: 0.35
  contour_fully_connected: 'high'
  minimum_pixels_contour: 1000


scale:
  threshold: 1.0
  contour_parameter: 0.50
  contour_fully_connected: 'high'
  minimum_pixels_contour: 10
