#opencv-python-headless==4.8.0.74  # For image processing (headless version, no GUI)
Pillow==9.3.0  # For handling image files in PIL format (used for DPI checks)
numpy>=1.21.0  # OpenCV relies on numpy for array handling
argparse  # Built-in library, not needed to be specified for modern Python versions but included for completeness
